# Decision Log — Project Ariadne PRD

Canonical memory and audit trail for the Ariadne PRD run. Every decision, change,
and override is recorded here as the conversation unfolds.

## Inputs loaded at activation

- `brief-zacs-cv-website-2026-06-10/brief.md` (shared brief, covers Theseus + Ariadne)
- `brief-zacs-cv-website-2026-06-10/addendum.md` (framework decision, doc strategy, backroom UX detail)
- `scratch/ariadne-planning-roadmap.md` (cross-session planning tracker)
- `_bmad-output/project-context.md` (persistent fact — project tech rules)

## Decisions

- 2026-06-23 — Intent: **Create** (no existing Ariadne PRD; only Theseus PRD present).
- 2026-06-23 — Workspace bound to `prd-project-ariadne-2026-06-23/`.
- 2026-06-23 — Working mode: **Fast path** (draft full PRD, `[ASSUMPTION]` tags where inferred).
- 2026-06-23 — Research subagents: **skipped deliberately.** Framework research already done in
  brief; markdown-pipeline choice is explicitly deferred to architecture (not to be decided here);
  easter-egg is a known pattern. Web research would not change a capability-level PRD and would
  itself be the over-investment the brief warns against.
- 2026-06-23 — **Q1 Docs curation scope: Representative first cut.** Curate a bounded high-impact
  subset of the Theseus trail (framework decision, best ADRs, headline pragmatism calls); backroom
  grows as ongoing WIP. Comprehensive curation explicitly rejected as the named failure mode.
- 2026-06-23 — **Q2 Backroom v1 boundary: Lean render + nav + syntax highlighting.** No search,
  no diagrams, no interactive extras in v1.
- 2026-06-23 — **Q3 Content assets: in hand, staged in `scratch/`.** Photo `avatar pic zac.jpg`
  (2026-03-03) and CV `Zac-Braddy-20260522.pdf` (2026-05-22). Content epic = wire photo, swap CV
  PDF, bring on-site copy to parity with the CV. No asset-sourcing work item.
- 2026-06-23 — **Post-draft additions (Zac).** Three content-refresh items not in the brief, folded
  into Feature 4.1: (a) remove the Twitter social link (`socials.tsx`) — no longer used; (b) remove
  the "The Reactionary" content item (`content/page.tsx`) — too old to be relevant; (c) refresh the
  About-me stats (Age 39→41) and rewrite the summary, which currently reads as the _old_ CTO/
  technical-leader positioning, to the current We Right Code positioning (0-to-1 Builder & System
  Modernisation Specialist; lead with what gets built). FR set renumbered to FR-1..FR-11 (still draft).
- 2026-06-23 — **Flagged for Zac's call:** the visible Twitter icon removal does not by itself touch
  the `twitter:` OG **card metadata** (`creator: '@zackerthehacker'`) on every page + layout, which
  governs X link-preview cards regardless of whether Zac posts. Left in place pending decision;
  logged as Open Question 7.
- 2026-06-23 — **Repo visibility: RESOLVED.** Zac confirms the GitHub repo is public and will remain
  public. Open Question 6 closed; the "repo public" assumption promoted to fact (FR-11 link resolves).
- 2026-06-23 — **Easter-egg emit mechanism (Zac raised the visibility concern).** Decision: on-load
  `console` emit is the primary mechanism. Rationale: browsers retain console messages logged before
  dev tools were opened and display them retroactively, so the on-load emit already satisfies "visible
  when they open dev tools" — Zac's concern is addressed without fragile detection. Dev-tools-open
  detection (heuristic, unreliable) is kept only as an **evidence-driven fallback**: add a re-emit
  _only if_ a target browser is found not to retain the buffer during the story. Delivery-first per
  §8 — simplest robust path first. Captured in FR-11 Approach + Open Question 4.
- 2026-06-23 — **Analytics coverage of new pages (Zac).** Clarified the distinction: traffic is not a
  KPI, but it must still be _collected_. New Backroom pages must hook seamlessly into the **existing**
  GA setup (`@next/third-parties` `GoogleAnalytics` in `layout.tsx`) so backroom traffic is available
  post-delivery / to inform future decisions. No new analytics system, no custom events, no GA
  re-architecture. Low/zero effort: backroom routes live under the same root layout, so App-Router
  page-view tracking should fire automatically — to be verified in-story. Updated §5, §6.1, §6.2, §7,
  §9, and FR-7 consequences.
- 2026-06-23 — **Open Question 7 resolved (Zac): remove Twitter card metadata pointer.** Remove the
  dead-account `creator: '@zackerthehacker'` field from the `twitter:` metadata in all five files
  (`layout.tsx`, `page.tsx`, `about-me/page.tsx`, `resume/page.tsx`, `content/page.tsx`). **Keep** the
  rest of each card block (`card`/`title`/`images`) so shared links still preview richly on X —
  recommended over nuking the whole block, since the card benefits Zac regardless of whether he posts;
  only the abandoned-account pointer is the liability. Folded into FR-5 consequences.
- 2026-06-23 — **Finalize run.** Input reconciliation (brief/addendum/roadmap) + quality-rubric
  review run as parallel subagents (outputs: `reconcile-*.md`, `review-rubric.md`). Gate verdict:
  PASS-WITH-NITS; reconciliation clean. Folded in 6 targeted fixes protecting the revenue rationale:
  (1) AI/BMAD workflow made a visible FR-6 content requirement; (2) site-as-live-sales-demo elevated
  in §1 + §2.1; (3) "permanent WIP by design / unplanned ≠ dropped" added to §6.2; (4) FR-5 file list
  marked re-grep-don't-trust; (5) Builder→Modernisation→Strategy gradient carried into §10 backroom
  voice; (6) WIP-cadence flavour restored. FR numbering verified contiguous (1–11), all cross-refs
  resolve. No phase-blockers — remaining Open Questions 1–5 correctly deferred to architecture/UX/
  story. Polish kept light by design (delivery-first guardrail; rubric already clean).
- 2026-06-23 — **PRD finalized: `status: final`.** Artifact ready as input to `bmad-ux` and
  `bmad-create-architecture` (per the Ariadne roadmap).
- 2026-06-23 — **Q4 Time-box: no estimates/time-box.** Project finishes regardless. The "time-box"
  is a _delivery-first pragmatism principle_, not a deadline: simple fixes get simple solutions;
  fix bugs/quirks when seen; complex problems that threaten delivery get deferred or worked around;
  no naval-gazing on project structure. Captured as a Constraints & Guardrails section, not an SM.
