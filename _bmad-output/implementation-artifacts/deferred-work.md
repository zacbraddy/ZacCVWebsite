# Deferred Work

## Deferred from: code review of story-1.1 (2026-06-11)

- `next start` script is dead under `output: 'export'` (no server to start) — `package.json`. Low-priority footgun; revisit only if static export is ever dropped.
- ~~`prepare: "husky"` fails under `npm ci --omit=dev` (prod/CI-only install, husky is a devDependency) — `package.json`. Deferred to Story 1.7 (Netlify deploy config), where the build/install command is defined.~~ **RESOLVED 2026-06-15 (Story 1.7)** — `HUSKY = "0"` set in `netlify.toml [build.environment]` neutralises the prepare hook during the Netlify install step without touching the `prepare` script (local hooks keep working). See ADR 0014.
- Geist fonts are fetched from Google Fonts at build time (`next/font/google`), so offline/air-gapped builds fail — `src/app/layout.tsx`. Reproducibility dependency; deferred to Story 1.7 (deploy/build environment).

## Deferred from: code review of story-1.2 (2026-06-11)

- ~~Prettier pinned `^2.8.7` alongside modern `eslint-config-prettier ^10` — `package.json`. Not a functional conflict (`eslint-config-prettier` is version-agnostic), but a currency smell carried over from the Story 1.1 scaffold; revisit when next touching the formatter.~~ **RESOLVED 2026-06-11** — Prettier bumped to `^3.8.4`, `pretty-quick` to `^4.2.2`; see ADR 0007.
- `npm run lint` only warns, never fails (no `--max-warnings 0` / CI gate), so genuine a11y/Next issues pass green — `eslint.config.mjs`. Already recorded as an open follow-up in ADR 0007; decide on a commit/CI gate later.

## Deferred from: code review of story-1.4 (2026-06-12)

- ~~No `.dark` class hook — dark is defined on `:root` only; the cascade can add `.light` but never re-assert dark on a subtree — `src/app/globals.css`. Latent; resolve in Story 1.5 (next-themes `attribute="class"`). Correct today (no class → dark).~~ **RESOLVED 2026-06-12 (Story 1.5)** — `next-themes` wired with `attribute="class"` now writes `class="dark"`/`class="light"` on `<html>`; the `.dark` class is present and re-asserts dark on any subtree. See ADR 0011.
- ~~No FOUC / pre-hydration guard — static export ships no theme class, so a future localStorage-backed `.light` toggle will flash dark-then-light on load (the full-viewport `body::before` gradient makes it visible) — `src/app/globals.css`. The pre-hydration blocking script is Story 1.5 scope (AC8); flag so 1.5 doesn't forget it.~~ **RESOLVED 2026-06-12 (Story 1.5)** — `next-themes`' blocking pre-hydration `<script>` (verified in `out/index.html`) sets the `<html>` class before first paint, plus `suppressHydrationWarning` on `<html>`. No palette flash. See ADR 0011.
- ~~Unlayered `body`/`html`/`*`/`a` rules sit outside any `@layer`, so they outrank the `@utility` token classes (utilities layer) — `src/app/globals.css`. A `bg-*`/`text-*` utility applied to `body` or `html` would silently lose to the hand-written rule. Matches archive behaviour; revisit only if a tier port needs to theme `body`/`html` via a utility.~~ **RESOLVED 2026-06-15 (Story 2.1 code review)** — surfaced as a real parity regression: the unlayered `* { margin: 0 }` reset was overriding the layout frame's `lg:mx-auto`, so the whole shell rendered pinned top-left instead of centred. Wrapped the bare `html`/`body`/`body::before`/`*`/`a` rules in `@layer base` so Tailwind utilities win. Verified in compiled CSS.

## Deferred from: code review of story-1.5 (2026-06-12)

- ~~Static `aria-label="Dark mode switch"` does not reflect the toggle's current/target state — a screen-reader user in light mode still hears "Dark mode switch", and the accessible name never updates — `src/components/atoms/theme-toggle.tsx`. Genuine latent a11y issue, but carried over verbatim from `archive/src/components/theme.js` and mandated character-for-character by Story 1.5 AC5 as the parity contract; changing it now would break parity. Revisit in the Ariadne a11y/polish pass (e.g. a state-aware label like "Switch to light mode" / "Switch to dark mode").~~ **RESOLVED 2026-06-12 (during review, Zac's call)** — label is now state-aware: `resolvedTheme === 'light' ? 'Switch to dark mode' : 'Switch to light mode'`, keyed on `=== 'light'` so the `undefined`→dark-default first render stays stable (AC4 hydration property preserved; verified `out/index.html` emits "Switch to light mode" with the moon). A deliberate, conscious step off strict AC5 parity — screen-reader-only, invisible to the Story 4.1 visual-diff gate, consistent with the idiomatic-Next protocol.

## Deferred from: preview verification of story-1.7 (2026-06-15)

- Near-instant icon-less flash on the theme toggle on load — `src/components/atoms/theme-toggle.tsx`. The `useSyncExternalStore` hydration guard renders the button without an icon until hydration (the deliberate trade for never showing a _wrong_ icon for the persisted theme). Sub-100ms cosmetic blip on a top-left utility button. **Consciously accepted as not worth resolving now (Zac's call, 2026-06-15)** — far higher-priority migration work outstanding. If ever worth removing the flash: render both icons in the static markup and switch with a CSS `dark:`-class variant (needs Tailwind v4's `dark` variant wired to the `.dark` class), which keeps the icon visible pre-hydration without a hydration mismatch — but note that approach can't fix the `aria-label`, so the guard would still be needed for the label. Revisit in an Ariadne polish / Story 4.1 pass.

## Deferred from: code review of story-1.7 (2026-06-15)

- Image loader (`src/image-loader.ts`) needs a robustness pass when real raster/remote images first flow through it (Epic 2–3). Today only the site-relative skeleton SVGs (`/next.svg`, `/vercel.svg`) route through the loader, so none of the gaps below can trigger — fixing now would be speculative robustness against inputs that don't exist. When the portrait/content images land, sanity-check: (a) **remote `src`** (`https://…`) needs a `remote_images` allowlist in `netlify.toml` or the Netlify Image CDN rejects it (Next's custom-loader path bypasses `remotePatterns` validation); (b) **already-percent-encoded `src`** would be double-encoded by `URLSearchParams` (`%2F`→`%252F`) → 404; (c) **out-of-range `width`/`quality`** (incl. `quality={0}`, which passes the `?? 75` nullish guard) is forwarded verbatim to the CDN, which only accepts `q` 1–100 / integer `w`; (d) filenames with spaces encode as `+` not `%20` (form-encoding) — moot while files stay kebab-case. None are regressions; all are forward-looking hardening for when the loader does real work.

## Deferred from: code review of story-1.6 (2026-06-15)

- Analytics has no environment gating or cookie-consent mechanism — `src/app/layout.tsx` renders `<GoogleAnalytics gaId="G-F98QXJC4S0" />` unconditionally, so GA fires in dev/preview/prod alike with no `process.env` gate and no PECR/GDPR consent layer before cookies/network calls. Not a regression (archive `gatsby-plugin-google-gtag` behaved identically), but a real consideration for a UK-facing business site. Revisit as a dedicated story (env-gate + consent), independent of the parity work.
- Roboto loaded single-weight `400` → browser faux-bold (algorithmic synthesis) for any `<strong>`/`<b>`/`font-weight:700` text — `src/app/layout.tsx`. Parity-faithful: the archive `gatsby-plugin-google-fonts` also loaded only `Roboto:400`, so adding `700` would exceed Story 1.6's AC8 scope. Revisit at the Epic 3 typography work / Story 4.1 per-tier visual-diff gate if real bold cuts are desired.
- `metadataBase` and `openGraph.url` hardcode the production host `https://zackerthehacker.com` — `src/app/layout.tsx`. Correct (and spec-mandated verbatim) for production parity, but the Story 1.7 Netlify preview will emit production OG/Twitter absolute image URLs. Consider env-derivation (e.g. `NEXT_PUBLIC_SITE_URL`) if preview-deploy social-card correctness ever matters.

## Deferred from: code review of story-2.1 (2026-06-15)

- `AnimateOnChange` rapid-navigation out→in handshake can drop or mis-time the content swap — `src/components/atoms/animate-on-change.tsx`. The ported state machine assumes exactly one out-animation completes per navigation; bursting B→C while A's `pop-out` is still running can latch stale content or swap on the wrong bubbling event. Byte-identical to the archive logic (not introduced by this story) and only reachable once Epic 3 adds multiple navigable routes plus fast navigation. Revisit if/when route-transition glitches show up during Epic 3, or harden alongside the bubbling-guard decision (a `e.target === e.currentTarget` guard plus capturing the latest `children` on each out-trigger would address both). Related: the descendant-event-bubbling decision logged in the Story 2.1 review findings.

## Deferred from: code review of story-2.2 (2026-06-15)

- ~~Malformed Tailwind class `xl: mr-0` in the sidebar `<nav>` className — `src/app/layout.tsx`. The stray space makes `xl:` a dangling no-op prefix and `mr-0` unconditional, so the intended `xl:mr-0` margin-reset never applies.~~ **RESOLVED 2026-06-15 (during review, Zac's call)** — corrected to `xl:mr-0`. The space was plainly an unintended typo and porting archive bugs verbatim is exactly the anti-pattern the Theseus migration avoids; a conscious step off strict parity, recorded in ADR-0016. Rendered consequence (nav is `hidden lg:flex`): at `xl`+ the right margin now resolves to `0` (intended) instead of the leftover `mr-3.5` (14px), so the Theseus nav sits 14px further right at `xl`+ than the equally-bugged live site — flagged for the Story 4.1 gate as a deliberate, expected difference, not a regression.
- Active-link detection over-matches via `startsWith` (prefix collision) — `src/components/atoms/nav-link.tsx`. The rule `(to !== '/' && pathname.startsWith(to))` matches any path that merely _starts with_ the link target, so a future sibling route sharing a prefix (e.g. `/content` highlighting on `/contents-archive`, `/resume` on `/resume-x`) would falsely activate. Cannot fire today — only `/` exists — and the rule is **pinned verbatim by AC2** ("the active-match rule is preserved verbatim from the archive"), so it is a parity-locked, pre-existing flaw, not introduced here. Revisit when prefix-sharing routes appear (Epic 3) or at the Story 4.1 gate. Segment-safe form if elevated: `pathname === to || pathname.startsWith(to + '/')` (preserves sub-path activation like `/resume/...` without prefix false-positives).
- Primary nav lacks `<ul>/<li>` list semantics — `src/components/molecules/nav-links.tsx`. The four nav items plus the Download CV anchor render as a flat sequence inside `<nav>` with no list markup, so assistive tech gets no item count/structure. Ported verbatim from the archive (pre-existing); an a11y enhancement beyond the zero-regression parity scope (NFR6). Revisit in a holistic Ariadne accessibility pass rather than mid-parity-port.

## Deferred from: code review of story-2.3 (2026-06-16)

- `border-inverse` light-theme parity discrepancy — `src/components/atoms/portrait-image.tsx`. The portrait border is the first render of `border-inverse`. Dark `:root` value `fafafa` is intentionally invalid (no `#`) → `currentColor` (archive quirk, ADR 0010). Light `.light` value `#5a5a5a` is valid (set in Story 1.4) → renders solid `#5a5a5a`, whereas the archive light value `'5a5a5a'` (no `#`, invalid) → `currentColor`≈`#333`. So Theseus light portrait border ≈`#5a5a5a` vs archive ≈`#333` — a subtle but real difference introduced when 1.4 added a `#` to the light value but not the dark one. Root cause is in Story 1.4's token work, not this change. Already recorded in ADR 0017 §4 and deferred to the Story 4.1 visual-diff gate (live site is arbiter). Clean fix if elevated: make light match the archive's `currentColor` outcome (drop the `#`, or set `#333`).
- Remove the Twitter social link — `src/components/molecules/socials.tsx`. Zac no longer uses Twitter and wants the link gone (decided during the 2.3 code review, 2026-06-16). Currently ported verbatim from the archive (`faTwitter` + `aria-label="Twitter"` + `twitter.com` href); the icon resolves fine under `@fortawesome/free-brands-svg-icons@7.2.0`, so it renders correctly until removed. Deliberately deferred out of the zero-regression parity port — do it in the **content-update epic**, removing the link from this component **and the live site together** so the two stay consistent. When removed, also drop `faTwitter` from the brand-icons import.

## Deferred from: code review of story-2.4 (2026-06-16)

- ✕ close button has no positioning — `src/components/molecules/mobile-menu.module.css`. `.closeButton` sets size only (24px), so the cross renders inline at the top of the panel flow, above the nav items, rather than absolutely cornered as `react-burger-menu` rendered it. The archive `.bm-cross-button` CSS also carried size only — the library injected the absolute position — so the CSS-only port faithfully reproduces the archive _stylesheet_ but loses the lib-injected placement. The spec already routes exact ✕ visuals to the Story 4.1 visual-diff gate; confirm the cross placement there against the live site.
- Panel has no `overflow-y` for very short viewports — `src/components/molecules/mobile-menu.module.css` (`.panel`). The four nav items + Download CV will not overflow on realistic devices, so no scroll is needed today. Full overflow/scroll handling belongs to Story 2.5 (custom scrollbar + `react-custom-scroll` replacement); revisit there rather than adding ad-hoc `overflow-y` now.
- Download CV keeps the now-modal drawer open — `src/components/molecules/mobile-menu.tsx` + `src/components/molecules/nav-links.tsx`. The Download CV anchor intentionally has no `onClick` (archive parity, `target="_blank"` + `download`), so selecting it leaves `menuOpen` true. Because the `vaul` drawer is now modal (scroll-lock + focus-trap), browsers that ignore `target="_blank"` for `download` anchors and navigate in the same tab will return the user to a still-open, scroll-locked, focus-trapped modal (recoverable via `Esc`/overlay/✕). The archive drawer was non-modal so had no such consequence. Net-new interaction from the modal upgrade; revisit if mobile QA shows the same-tab fallback in practice, or fold into the Story 4.1 gate.

## Deferred from: code review of story-2.5 (2026-06-17)

- **Verify the route-change scroll-reset timing against the live site once the first content pages exist** — `src/components/molecules/content-transition.tsx:14-16`. The scroll-reset shipped as an imperative `scrollRef.current?.getScrolledElement()?.scrollTo(0, 0)` in a `[pathname]` effect (not the spec-prescribed changing-`scrollTo`-prop — that literal port trips the error-level `react-hooks/set-state-in-effect` rule that AC6 requires green; documented in ADR 0019(b), behaviourally faithful per NFR7). The timing nuance to check: the imperative call fires immediately on pathname change, scrolling the **outgoing** content to top while `<AnimateOnChange>` is still displaying it during its 100ms out-animation — whereas the archive's `scrollTo` prop only reached `<Scrollbar>` after the content swap, so it scrolled the **incoming** content. Net: a possible "outgoing page jumps to top mid-fade" not present on today's live site. **Trigger to revisit (do not forget):** once Epic 3 lands the first two content pages (real, overflowing, navigable routes), scroll down on page A, navigate to page B, and watch the transition in both themes against `zackerthehacker.com` — confirm there's no outgoing-page jump and the incoming page starts at the top. This is the Story 4.1 visual/behavioural gate item for the scrollbar. If a jump shows: fire the reset aligned with `<AnimateOnChange>`'s content swap (on new-content mount) rather than eagerly on `[pathname]`.

## Deferred from: code review of story-2.6 (2026-06-17)

- **Overlay positioning `absolute inset-0` — confirm full-viewport coverage at the Story 4.1 browser gate** — `src/components/atoms/loading-spinner.tsx:21`. `<LoadingSpinner />` is the first child of `<body>` with no positioned ancestor, so `absolute inset-0` resolves against the initial containing block (= the viewport), which covers the initial-load viewport correctly. The narrow gap: if page content ever exceeds the viewport during the loading window, an `absolute`-anchored overlay covers only the initial viewport, not the full scroll height. The spec deliberately prefers `absolute` for byte-faithfulness to the archive and gates the `fixed inset-0` swap on the browser check. Trigger: at the Story 4.1 visual gate, hard-reload/network-throttle with real Epic 3 content present and confirm the splash covers the viewport; swap to `fixed inset-0` (parity-neutral, zero dark-theme visual cost) only if a gap shows.
- **No timeout / JS-failure fallback — the overlay persists forever if hydration never runs** — `src/components/atoms/loading-spinner.tsx`. The atom only removes itself on `document.readyState === 'complete'` via the client store; if JS fails to load/hydrate entirely, the splash stays up indefinitely. Pre-existing behaviour (parity with the archive, which also only hid on `readyState complete` with no timeout). Revisit only if a hard-failure fallback is ever wanted (e.g. a max-duration `setTimeout` escape hatch) — out of the zero-regression parity scope here.
- **No `prefers-reduced-motion` guard on the new always-on Pac-Man animation** — `src/app/globals.css` (the static `@keyframes`) + `src/components/atoms/loading-spinner.tsx`. The Pac-Man chomp animates unconditionally for motion-sensitive users. Parity with the archive (which had no reduced-motion handling either), so not a regression — but a genuine a11y enhancement for a holistic Ariadne accessibility pass (a `@media (prefers-reduced-motion: reduce)` rule disabling the keyframes would address it).
- **AC6 in-browser visual parity not performed in this headless review** — the live-browser confirmation (Pac-Man animation playing in both themes, dark matching the live site exactly, light showing the light palette, removal-on-ready via hard-reload/throttle) was not run. Build, lint, static export and the prerendered overlay markup/keyframes were all verified programmatically. Routed to the Story 4.1 all-tier visual gate, per the story's own verification plan.

## Deferred from: code review of story-3.1 (2026-06-18)

- **`AnimateOnChange` named-keyframe path is dead — only the opacity-transition fade runs** — `src/components/atoms/animate-on-change.tsx` (consumed by the new `rotating-job-title.tsx`). When no `animation` base name is passed (the home rotating-title case), `animationIn`/`animationOut` resolve to `"undefinedIn"`/`"undefinedOut"`, so `styles.animation` is set to the literal invalid shorthand `"undefinedIn"` which the browser discards. The visible cross-fade therefore comes entirely from the `transition: opacity {durationOut}ms` path, which works correctly. **Not a regression** — the archive home also passed no `animation` base name, so this is faithful parity. Pre-existing in the Story 2.1 atom (Epic 2 frozen shell), not introduced by 3.1. Revisit only if a holistic animation refactor (Ariadne) wants the keyframe path actually wired or the dead-string branch cleaned up.
- **Rotating-title content only advances on `transitionend` — a missed event blanks the title for one ~4s cycle** — `src/components/atoms/animate-on-change.tsx` (consumed by `rotating-job-title.tsx`). Displayed text swaps in the `onTransitionEnd` handler; if the opacity `transitionend` is skipped (backgrounded/throttled tab where `setInterval` still fires but the transition is coalesced, or the page unmounting mid-fade during `ContentTransition`), the title can sit at `opacity: 0` until the next index tick re-triggers the out→in cycle (self-heals within ~4s; worst case one stuck/blank cycle, never a permanent hang). **Pre-existing** AnimateOnChange timing fragility (Epic 2 frozen shell), inherited from the archive design — not introduced by 3.1. Revisit alongside any future AnimateOnChange hardening; confirm at the Story 4.1 visual gate that no blank-title flash is visible against the live site.

## Deferred from: code review of story-3.2 (2026-06-22)

- **Multiple `<h1>` per page (heading-outline a11y)** — `src/components/atoms/heading.tsx`. The shared `Heading` atom hard-codes `<h1>`, and `/about-me` renders it four-plus times (About Me / What I Do / Testimonials / Things I Like), while `AbilityDescription` uses `<h2>` — so the document outline has sibling `<h1>`s and an H1→H2→H1 jump. Verbatim parity with the archive (`atoms/heading.js` was also `<h1>`); explicitly an Ariadne a11y-pass item per the story (NFR7), not a Theseus fix. Revisit in the holistic accessibility pass — likely give `Heading` an optional `as`/level prop so sections can be `<h2>` without disturbing parity elsewhere.
- **Carousel controls a11y: no accessible name, suppressed focus ring, keyboard-inert viewport** — `src/components/organisms/testimonials.tsx`. The prev/next buttons are icon-only (`faLessThan`/`faGreaterThan`) with no `aria-label`, so they announce as bare "button"; `focus:outline-none` removes the visible focus indicator with no replacement (WCAG 2.4.7); the Embla viewport `<div className="overflow-hidden" ref={emblaRef}>` has no `role`/`tabIndex` and isn't keyboard-scrollable. All verbatim parity (button classes are byte-identical to the archive). Ariadne a11y pass — add `aria-label`s, a visible `focus-visible` ring, and consider keyboard navigation for the carousel.
- **Testimonial grammar typo "he's be an asset"** — `src/components/organisms/testimonials.tsx` (Jamie Taylor quote). Reads "he's be an asset to any team". Verbatim from `archive/src/components/organisms/testimonials.js:125` (the straight `'` was correctly escaped to `&apos;` on port; the grammar is unchanged). Frozen content → Project Ariadne's content refresh, not a Theseus edit.
- **Author block can overflow the narrow card; `self-*` align utilities inert on an `absolute` element** — `src/components/molecules/testimonial.tsx`. The author block is `absolute … self-start lg:self-end break-words` with only `style={{ bottom: '1rem' }}` — `self-start`/`self-end` (align-self) do nothing on an absolutely-positioned element, and no `left`/`right`/`inset-x` is set, so the horizontal anchor is the static-position fallback. A long `- author, jobTitle, company` string can clip/overflow the mobile `w-64` card before `break-words` wraps. Verbatim parity (matches `archive/.../testimonial.js:12-13`, including the `relative`-ancestor fix the spec sanctioned for Embla). Confirm the author placement against the live site at the Story 4.1 visual gate (Carousel parity risks #1/#2); adjust which wrapper carries `relative`, or add `right-*`/`inset-x`, only if a divergence shows.
- **`sm:grid sm:grid-cols-2 lg:block` wraps a single child — the two-column intent never materialises** — `src/components/organisms/about-me.tsx`. The outer grid has one child (`grid grid-rows-6` of six `StatRow`s), so at `sm` the implied two-column split can never happen. Verbatim from `archive/src/components/organisms/about-me.js:76-77`, so faithful parity, not a regression. Revisit only if Ariadne reworks the stats-block layout.

## Deferred from: dev of story-3.3 (2026-06-22)

- ~~**In-browser visual parity not performed in this headless dev run (AC#5)**~~ **RESOLVED 2026-06-22 (Zac's local visual check)** — Zac reviewed the changes locally and confirmed `/resume` looks good visually. The headless dev-run caveat below is therefore satisfied for routine sign-off; the formal all-tier visual diff (both themes, desktop + mobile, vs `zackerthehacker.com`) still rolls up to the Story 4.1 gate alongside every other page. — the live-browser confirmation (the seven experience items, the timeline divider's two dots + `bg-tertiary` connector and vertical placement, the `md:` two-column split, the `lg` `TimelineTimeCompany` swap, sorted skill pills — in **both themes**, **desktop + mobile**, against `zackerthehacker.com`) was **not** run interactively. Verified programmatically instead: `npm run build` green + pure static export (`/resume` = `○ (Static)`), `npm run lint` clean, `out/resume.html` contains `<title>Resume - Zac Braddy</title>` + all four headings + all seven companies + the seven certs + frozen typos (`MonogoDB`/`MSCD`/`Programatic`) + the skill pills, and the divider CSS is emitted **from the CSS Module** (hashed `…__timelineDivider` + `:before`/`:after` pseudo-elements). The **`padding-top` cascade** (Divider parity risk) was resolved by inspecting the emitted stylesheets: `.py-4` lives in the global Tailwind chunk loaded **first**, the module's `padding-top:4rem` in a chunk loaded **last** → equal specificity, later source wins → **module's `4rem` wins** (intended/archive result; `py-4`'s `padding-bottom:1rem` still applies), so no reconciliation and **no ADR 0024** was needed. Final all-tier visual sign-off routed to the **Story 4.1 gate**, per the story's verification plan.

## Deferred from: code review of story-3.3 (2026-06-22)

- **Unverified certification acronyms + job-title casing (frozen content → Ariadne)** — `MSCD: Web Applications` and `MCSP: Microsoft Certified Professional` in `src/components/organisms/certifications.tsx` read like transposed/garbled acronyms (likely `MCSD` and an `MCP`/`MCSA`-family cert), and the Odondo title casing `(Python-react)` in `src/components/organisms/experience.tsx` is inconsistent with `(Node-React)`/`(Node-Svelte)` elsewhere. Deferred (not patched in Theseus) because the acronyms touch Zac's actual certification facts and need his verification rather than a guess, and the casing is cosmetic copy polish — both belong to the Ariadne content refresh. (The two unambiguous spelling typos in the same review — `MonogoDB`→`MongoDB`, `Programatic`→`Pragmatic` — were fixed in place per [[theseus-fix-bugs-dont-port-verbatim]].)
- **Inert / redundant Tailwind classes carried over verbatim from the archive** — `flex flex-flow-col flex-wrap` in `src/components/molecules/skills-list.tsx` (`flex-flow-col` is not a real Tailwind utility, emits no CSS, container falls back to default `row`); the `flex` + `block`/`lg:block` display redundancy in `src/components/atoms/timeline-time-company.tsx` (both display utilities present on the same element); and `static py-4 top-0 bottom-0 bg-tertiary` on the divider in `src/components/organisms/timeline-item.tsx` (`top-0`/`bottom-0` are inert on a `static` element). All three are byte-identical to the archive originals, render correctly on the live site today, and have zero visual impact — confirmed by the Edge Case Hunter against `archive/src/...`. **Critical guardrail:** do NOT "fix" `flex-flow-col` into `flex-col` — the live layout is the `row` fallback, so changing it would break visual parity. The only parity-neutral cleanup is deleting the dead classes outright. Cosmetic tidy-up for a holistic Ariadne pass; not a Theseus fix.
- **Heading-outline / semantic-markup a11y on the Resume page** — the shared `src/components/atoms/heading.tsx` (hard-coded `<h1>`) is reused four times on `/resume` (Resume / Experience / Certifications / Other Skills), compounding the multiple-`<h1>` document-outline issue **already tracked from the story-3.2 review** (same root cause, same atom). Plus: each timeline entry's job title renders as a non-semantic `<div className="font-bold">` rather than a heading element (`src/components/organisms/timeline-item.tsx`), and the purely-decorative `TimelineDivider` (`src/components/atoms/timeline-divider.tsx`) carries no `aria-hidden`. All verbatim parity with the archive. Fold into the same holistic Ariadne accessibility pass as the 3.2 heading item — likely giving `Heading` an optional `as`/level prop so sections can be `<h2>` without disturbing parity elsewhere.

## Deferred from: dev of story-3.4 (2026-06-22)

- **Three Zac-approved content carve-outs on `/content` — conscious, expected deviations from the live Gatsby site (NOT regressions)** — folded into the Story 3.4 spec on 2026-06-22 and implemented: (1) the **Tabs & Spaces** card now carries a Spotify `link` (`https://open.spotify.com/show/4Xqo3bTXOqyrlPjX9H2Ytk?si=55457f85cf1c4014`) where the archive card had none; (2) **`Youtube` → `YouTube` casing** in the item-6 highlight title and the two `youtube channel` mentions in item-7 body copy (the lowercase quoted `"youtuber"` is deliberately **kept**); (3) the page heading `Content I've created` → `Content I've Created` (capital C, to match the page `<title>` casing). Accepted because Gatsby retires at cutover and Theseus becomes the source of truth — same pattern as the `xl:mr-0` (ADR 0016) and theme-toggle aria-label deviations. **Flag all three at the Story 4.1 visual gate as expected differences vs `zackerthehacker.com`, not parity failures.** Editorial, not architectural — no ADR.
- **In-browser visual parity not performed in this headless dev run (AC#2/#6, the Thumbnail parity risk)** — the live-browser confirmation (seven cards in archive order; the **thumbnail sizing/cropping** in the desktop `md:w-48`×`md:h-36` cover-cropped box vs the stacked ≤300px mobile layout; the alternating left/right desktop layout collapsing to one column below `md`; `border-inverse` card + divider borders flipping side with `order`; all seven external links opening in a new tab — in **both themes**, **desktop + mobile**) was **not** run interactively here. Verified programmatically instead: `npm run build` green + pure static export (`/content` = `○ (Static)`, no `.func`), `npm run lint` clean, and `out/content.html` contains `<title>Content I've Created - Zac Braddy</title>`, the capital-C heading, all seven highlighted titles (incl. `YouTube`), all seven body blocks (incl. `YouTube channel` ×2 + kept lowercase `"youtuber"`), **all seven** external `href`s (incl. the Spotify link), and the seven `next/image` outputs resolving through the Netlify loader (`/.netlify/images?url=%2Fimages%2F…`). The GatsbyImage→`next/image` thumbnail reconciliation (ADR 0024) is the one genuine rendering judgement and **needs a dot-for-dot live check** — routed to the **Story 4.1 gate**, per the story's verification plan.
- **Story Dev Note was wrong that double-quotes don't trip `react/no-unescaped-entities` (resolved in place, render-identical)** — the 3.4 Dev Notes asserted the straight double-quotes in `"youtuber"` and `"The Reactionary"` could be left literal because the lint rule "does not trip" on `"`. This repo's `eslint-config-next` **does** flag bare `"` (`react/no-unescaped-entities`), so `npm run lint` failed on 4 occurrences. Fixed by escaping to `&quot;`, which renders byte-identically to `"` — so **rendered parity is preserved** and the quirk is intact; only the JSX source representation differs (same treatment as the `&apos;` apostrophe-escaping convention, [[theseus-epic3-jsx-apostrophe-escaping]]). No content change, no deferral needed beyond this note.
- **Multiple-`<h1>` / decorative-image a11y on `/content` (same Ariadne bucket as 3.2/3.3)** — the shared `Heading` atom (`<h1>`) is used once here, adding to the cross-page multiple-`<h1>` document-outline item already tracked from 3.2/3.3; and each `ContentThumbnail` uses `alt=""` (faithful: decorative thumbnail inside a labelled link). Both are verbatim-parity calls — fold into the same holistic Ariadne accessibility pass, do not fix in Theseus (NFR7). The card border is `border-inverse`, so the **story-2.3 `border-inverse` light-theme parity item** also applies to these cards (Story 4.1 gate).

## Deferred from: code review of story-3.4 (2026-06-22)

- **Thumbnail visual parity — the ADR-0024 reconciliation needs a dot-for-dot live check (confirms + sharpens the dev's Thumbnail-parity-risk deferral above)** — `src/components/atoms/content-thumbnail.tsx`, `src/components/organisms/content-item.tsx`. The code review's three adversarial layers converged on the same one open item the dev already flagged (line 96): the non-verbatim `next/image` className (`w-full max-w-[300px] h-auto object-cover md:h-full md:w-48 md:max-w-none`) reproduces Gatsby `CONSTRAINED` only on judgement, so it must be eyeballed against `zackerthehacker.com` (both themes, desktop + mobile). **Two specifics to look at at the 4.1 gate, surfaced by the Edge Case / Blind hunters:** (a) the **portrait `course` 360×450** thumbnail is the most aggressively `object-cover`-cropped image in the fixed desktop `md:w-48`×`md:h-36` (192×144, 4:3) box — confirm its cover art doesn't lose meaningful pixels vs the live render; (b) on mobile (`<md`) each card's thumbnail height differs (square `tabsAndSpaces` ≈300px, portrait `course` ≈375px) and the verbatim desktop `md:h-32` text column has no overflow handling so the longest copy (The Reactionary, Tabs & Spaces) could clip — both are **faithful-to-archive quirks** (NFR7), not regressions, but worth a conscious sign-off. No code change in Theseus; verification only.
- **Dismissed by this review (recorded so they aren't re-raised) — all verbatim-archive parity or layer-verified-safe, no action:** (1) `ContentItem` types `link?` optional and renders `<a href={link}>` — spec-mandated optional type (Task 3), verbatim archive, and now unreachable since carve-out #1 gives all seven items a link; (2) whole-card-is-one-link verbosity for screen readers — verbatim archive structure, Ariadne a11y bucket; (3) `order="right"` `md:items-end` doesn't set `text-align` on the prose — verbatim archive quirk; (4) the `--spacing-118`/`md:w-118` magic-number coupling — executes accepted ADR 0023, Edge Case Hunter verified it resolves under Tailwind v4; (5) the unstyled `Oops…:S` fallback `<div>` — verbatim NFR7, unreachable (all seven keys match); (6) the image loader hardcoding `/.netlify/images` off-dev would 404 on a non-Netlify host — pre-existing Story-1.7 loader coupling, by-design (site is on Netlify), already in the loader-robustness deferral; (7) `&quot;` escaping vs the spec's literal data map — render-identical, already noted at line 97. CLS, the Server-Component boundary, `rel="noreferrer"`, and `md:w-118` resolution were all explicitly verified **safe**.

## Deferred from: dev of story-3.5 (2026-06-22)

- **`404: Not found` (title) vs `404: Not Found` (heading) casing quirk — preserved verbatim, Ariadne's domain** — `src/app/not-found.tsx`. The document `<title>` carries lowercase "found" (`404: Not found - Zac Braddy`) while the visible `<h1>` carries capital "Found" (`404: Not Found`) — a deliberate-looking inconsistency in the archive (`archive/src/pages/404.js`: `Seo title="404: Not found"` + `<h1>404: Not Found</h1>`), preserved exactly per NFR7. **Zac confirmed (2026-06-22): preserve verbatim, no carve-out** — this is authored copy (frozen content, Project Ariadne's editorial domain), unlike the ADR-0021 doubled-`<title>` fix which corrected a mechanical react-helmet artefact. Do **not** "fix" the casing to match. Routed to Ariadne / the Story 4.1 gate. _(If Zac later wants the casing aligned, that's a one-line carve-out — his call.)_
- **Inherited (not archive-matching) og:title/twitter:title on the noindex 404 — immaterial, accepted delta** — `src/app/not-found.tsx`. Only `title: { absolute: '404: Not found - Zac Braddy' }` is set; `description`/`openGraph`/`twitter`/OG-image/favicon **inherit** the Story-1.6 root defaults (NFR6 anti-gold-plating, **Zac confirmed 2026-06-22**). So the Theseus 404 emits the inherited `og:title` (`Zac Braddy - CV website`) where the archive's `Seo` emitted the raw unsuffixed `404: Not found` (react-helmet's `titleTemplate` never applied to meta tags). The page is `noindex` (Next auto-injects it) and never shared, so this is **not a perceptible/functional regression** — and Zac is removing Twitter entirely in the next project, so re-declaring its meta now would be pure gold-plating. Logged as an immaterial, expected delta for the Story 4.1 gate; do **not** "fix" it.
- **Body-text centring — Zac-approved conscious deviation from verbatim parity (NOT a regression)** — `src/app/not-found.tsx`. Added `text-center` to the paragraph (`<p className="text-tertiary sm:text-2xl text-center">`). The archive/live 404 wraps `<h1>`+`<p>` in `flex flex-col items-center`, which centres each child _as a block_ but leaves the **paragraph's wrapped lines left-aligned** (ragged) when the copy wraps on narrower viewports — a real quirk on `zackerthehacker.com`. `text-center` centres the wrapped lines. **Zac requested this directly (2026-06-22)** — a deliberate step off NFR1 byte-for-byte parity, valid because Theseus becomes the source of truth at cutover (same category as `xl:mr-0`/ADR 0016 and the 3.4 content carve-outs). **No ADR** (one-utility visual tweak, not architectural — NFR6). **Flag at the Story 4.1 visual gate as an expected, intended difference vs the live site, not a parity failure.** Re-verified after the change: lint clean, build green + pure static export, `out/404.html` still the CUSTOM page with `<p class="text-tertiary sm:text-2xl text-center">` + templated title intact.
- **In-browser visual parity not performed in this headless dev run (Task 2 final sub-item)** — the live-browser confirmation (a deliberately bogus URL rendering the centred `404: Not Found` heading + `...the sadness...` paragraph **inside the persistent shell** — sidebar/nav/content-pane/route-transition — in **both themes**, **desktop + mobile**, with the tab title `404: Not found - Zac Braddy`, against `zackerthehacker.com/<bogus>`) was **not** run interactively. Verified programmatically instead: `npm run build` green + **pure static export** (all routes `○ (Static)`, no `.func`), `npm run lint` clean, and the must-verify gate — `out/404.html` is the **CUSTOM** page (not Next's default grey 404): it contains the `404: Not Found` heading (capital F), the `You just hit a route that doesn&#x27;t exist...the sadness...` paragraph, `<title>404: Not found - Zac Braddy</title>` (lowercase "found"), the surrounding **layout shell** (`bg-primary-400` content pane + `nav` chrome), and the Next-auto-injected `noindex`. Final all-tier visual sign-off routed to the **Story 4.1 gate**, per the story's verification plan.

## Deferred from: code review of story-3.5 (2026-06-22)

- **Inherited `og:title`/`twitter:title` on the noindex 404 — reaffirmed by code review (already a Zac-confirmed dev-phase decision; do NOT re-raise)** — `src/app/not-found.tsx`. The bmad-code-review Edge Case Hunter independently surfaced the same delta the dev logged above (the 404 emits the inherited `og:title`=`Zac Braddy - CV website`, not the archive's raw `404: Not found`), rating it Medium and noting it's inconsistent with the per-page OG pattern in `page.tsx`/`about-me`. The Acceptance Auditor and the story's "Decisions confirmed with Zac" #2 confirm this is a **deliberate, settled** inherit-root-defaults choice (NFR6: page is `noindex`/unshared, Twitter being removed next project — re-declaring is gold-plating). Conclusion: not a new action — immaterial, accepted delta routed to the **Story 4.1 gate**. _(If Zac ever wants strict OG parity: add `openGraph: { title: '404: Not found' }` + `twitter: { title: '404: Not found' }` — his call.)_
- **Dismissed by this review (recorded so they aren't re-raised) — all refuted or working-as-designed, no action:** (1) Blind Hunter's "`not-found.tsx` metadata may be ignored" — refuted, `out/404.html` carries the templated `<title>` (verified); (2) "`{ absolute }` hard override" — by design (ADR 0021 root-segment exception); (3) "`h-full` ancestor-chain dependency" — refuted, same `ContentTransition → div.h-full` chain every ported page uses, verbatim parity; (4) "custom classes may not resolve" — refuted, `font-fancy-heading`/`text-secondary`/`text-tertiary` defined in `globals.css` and present in `out/404.html`; (5) Auditor's "`text-center` non-verbatim" — Zac-approved documented exception (see dev-phase entry above), AC#1 otherwise MET; (6) Auditor's "AC#4 build evidence not reproducible from diff" — satisfied by direct dev-session build/lint/`out/404.html` verification.
