const SectionLabel = ({ children }: { children: React.ReactNode }) => (
  <p className="mt-[14px] mb-[6px] mx-[22px] text-[10.5px] font-bold tracking-[0.14em] uppercase text-dim opacity-70">
    {children}
  </p>
);

export default SectionLabel;
