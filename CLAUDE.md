# CLAUDE.md

## Project Context

Before implementing any code in this repository, read
[`_bmad-output/project-context.md`](_bmad-output/project-context.md). It contains the
critical, project-specific rules and gotchas AI agents must follow (tech stack, atomic
design structure, the CSS-variable theming system, PurgeCSS/Tailwind pitfalls, and more).

Follow those rules exactly. When a requested change conflicts with one, flag it rather than
silently breaking it.
